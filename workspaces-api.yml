spring:
  application:
    name: workspaces-api

---
# Items that apply to ALL profiles:
eureka:
  instance:
    appname: workspaces-api
server:
  port: 9090

---
spring:
  profiles: workspaces-api-01
eureka:
  instance:
    appname: workspaces-api
server:
  port: 9091


